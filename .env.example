# Set private values locally and in Vercel. Never use NEXT_PUBLIC_ for these.
APP_PASSWORD=
SESSION_SECRET=
# JSON array of objects with stable id, name, adult emoji
FAMILY_MEMBERS=[]
# Optional locally; required on Vercel
BLOB_READ_WRITE_TOKEN=
# Keep preview and production isolated
COMMENT_NAMESPACE=

# OpenRouter free-model summaries (server only)
OPENROUTER_API_KEY=
